app = "whatsapp-bridge-api"
primary_region = "iad" # MUDAMOS PARA VIRGINIA

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 0

[[mounts]]
  source = "whatsapp_data"
  destination = "/data"

[vm]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 1024
